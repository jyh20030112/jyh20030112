[project]
name = "jyh20030112"
version = "0.1.0"
description = "Automated commit and language statistics for a GitHub profile README"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
]

[tool.uv]
package = false
